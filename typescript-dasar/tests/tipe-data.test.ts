describe("Tipe Data", () => {
    it("should be able to use string", () => {
        const name: string = "John Doe";
        const balance: number = 1000;
        const isVip: boolean = true;
        
        console.log(name);
        console.log(balance);
        console.log(isVip);
    });
});